---
const { title, description, image, tools, date } = Astro.props;
---

<div
  class="w-[80%] mx-auto box-border flex mt-8 flex-col md:grid overflow-hidden card my-4 bg-slate-50 rounded-xl"
>
  <img src={image} alt="Web devguru" />
  <div
    class="box-border px-4 py-2 text-slate-900 md:text-white md:bg-opacity-60 md:bg-black content md:rounded-xl bg-clip-padding backdrop-filter backdrop-blur-sm"
  >
    <h1 class="text-xl font-bold">
      {title}
    </h1>
    <span class="mt-8">{date}</span>
    <p class="mt-3">{description}</p>
    <p class="mt-2 text-xs">
      {tools}
    </p>
  </div>
</div>
