---
const { title, image } = Astro.props;
---

<div class="flex flex-col items-center justify-between h-20 m-2 sm:flex-row">
  <img class="rounded-full" src={image} width="50px" alt={title} />
  <h1 class="mx-4">
    {title}
  </h1>
</div>
